#!/usr/bin/env bash
# .sh/bin/setup-all 20170519 - 20190430
# Copyright (C) 1995-2019 Mark Constable <markc@renta.net> (AGPL-3.0)

export PATH=/bin:/usr/bin:/sbin:/usr/sbin:/root/.sh/bin

[[ $1 =~ '-h' ]] && echo "Usage: setup-all [hostname.domain.name] [(mysql)|sqlite] [IP]" && exit 1

VHOST=${1:-$(hostname -f|tr 'A-Z' 'a-z')}
DTYPE=${2:-'mysql'}
USEIP=${3:-''}

HNAME=${VHOST%%.*} # hostname
PNAME=${VHOST#*.*} # primary domain

echo "### Entering setup-host as $VHOST with $DTYPE"
setup-host $VHOST $DTYPE $USEIP
echo "### Entering setup-deb"
setup-deb
echo "### Entering setup-db"
setup-db
echo "### Entering setup-etc"
setup-etc
echo "### Entering setup-hcp"
setup-hcp
echo "### Add primary domain.name"
addvhost $PNAME

source /root/.vhosts/$PNAME

MAILTO=${MAILTO:-$AMAIL}
echo "### Send message to: $MAILTO"
cat ~/.vhosts/$PNAME.conf ~/.vhosts/$VHOST.conf | mail -s "Setup $PNAME - $(hostname -i)" -r admin@$PNAME $MAILTO

systemd-cat -t hlog echo "$(whoami) $(basename $0) $*"
